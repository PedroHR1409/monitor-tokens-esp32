# AGENTS.md — Monitor.AI

Painel ESP32-S3 (LVGL) + daemon/coletor Python stdlib-puro.

**Leia primeiro: [`CLAUDE.md`](CLAUDE.md)** — fluxo de trabalho (AgentSpec SDD:
brainstorm → define → design → build → ship), comandos, regras duras e estrutura.

Regras complementares também valem: `.claude/sdd/features/` para o escopo em
andamento e `tools/README.md` para o mapa das ferramentas.
